'''
Operador AND:
True — True → True
True — False → False
False — True → False
False — False → False

Operador OR (exclusivo XOR):
True — True → False
True — False → True
False — True → True
False — False → False

Operador OR:
True — True → True
True — False → True
False — True → True
False — False → False

Verdade do NOT (negação)
NOT — True → False
NOT — False → True

'''
